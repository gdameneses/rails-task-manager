<h1>Todo list</h1>
<ul>
<% @tasks.each do |task| %>
  <li><h4><input type="checkbox"><%= link_to task.title, show_task_path(task.id) %></h4></li>
<% end %>
</ul>
<%= link_to '+ Add a new task', new_task_path %>
